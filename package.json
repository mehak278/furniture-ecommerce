{
  "name": "furnimart-workspace",
  "version": "1.0.0",
  "description": "Multi-Vendor Furniture E-Commerce Platform Workspace",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client && npm install --prefix admin && npm install --prefix vendor",
    "server": "npm run start --prefix server",
    "server-dev": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "admin": "npm run dev --prefix admin",
    "vendor": "npm run dev --prefix vendor",
    "dev": "concurrently \"npm run server-dev\" \"npm run client\" \"npm run admin\" \"npm run vendor\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
